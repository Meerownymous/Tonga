# Skills

Vendored from [mattpocock/skills](https://github.com/mattpocock/skills), MIT licensed
(see `LICENSE` in this directory).

Upstream commit: `3cca18b368ae95cdbdebbff572ccafa662551015`

Taken from the upstream `engineering/` and `productivity/` categories, flattened into
this directory so Claude Code discovers each `SKILL.md`. The upstream `misc/`,
`in-progress/` and `deprecated/` categories are not included.

Each skill directory also carries an `agents/openai.yaml` from upstream, which
configures the skill for Codex. It is unused by Claude Code.

To update, re-copy the two categories from a newer upstream checkout and record the
new commit above.
